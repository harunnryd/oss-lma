[package]
name = "oss-lma-desktop"
version = "0.1.0"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
app = { path = "../crates/app" }
tauri = { version = "2", features = [] }
