[package]
name = "lma-capture"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
hound = "3.5"

[target.'cfg(target_os = "macos")'.dependencies]
cidre = { version = "0.22.1", default-features = false, features = ["app", "async", "core_audio", "macos_13_0", "sc"] }
